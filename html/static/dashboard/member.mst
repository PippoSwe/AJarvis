{{#items}}
        <div id="list-member-{{member_id}}" class="list-group-item border-0 pb-4">
            <span class="float-left">{{firstname}} {{lastname}}</span>
            <div class="btn-group pull-right">
                <button type="button" data-id="{{member_id}}" class="delete close"><span class="ion-ios-close-outline"></span></button>
            </div>
        </div>
{{/items}}