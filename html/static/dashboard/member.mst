{{#items}}
        <div id="list-member-{{member_id}}" class="list-group-item pb-5">
            <h4 class="list-group-item-heading float-left">{{firstname}} {{lastname}}</h4>
            <div class="btn-group pull-right">
                <button type="button" data-id="{{member_id}}" class="delete btn btn-danger"><span class="ion-ios-trash pr-2"></span>Cancella</button>
            </div>
        </div>
{{/items}}