{{#items}}
        <div id="standup-{{id}}" class="list-group-item">
            <h4 class="list-group-item-heading float-left">{{standup}}</h4>
                <a type="button" data-id="{{id}}" href="../standup/index.html" class="go btn btn-primary">Vai<span class="ion-ios-arrow-right pl-2"></span></a>
        </div>
{{/items}}