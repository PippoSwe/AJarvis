{{#items}}
        <div id="standup-{{id}}" class="list-group-item pb-5">
            <h4 class="list-group-item-heading float-left">{{standup}}</h4>
            <div class="btn-group pull-right">
                <a type="button" data-id="{{id}}" href="../standup/index.html" class="go btn btn-primary"><span class="ion-ios-trash pr-2"></span>Vai</a>
            </div>
        </div>
{{/items}}