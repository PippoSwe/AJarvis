{{#items}}
        <div id="standup-{{id}}" class="list-group-item">
            <h4 class="list-group-item-heading float-left">{{standup}}</h4>
            <div class="btn-group pull-right">
            	<button type="button" data-id="{{id}}" class="delete btn btn-danger" data-toggle="modal" data-target="#exampleModalCenter"><span class="ion-ios-trash pr-2"></span>Cancella</button>
            	<button type="button" data-id="{{id}}" href="../standup/index.html" class="go btn btn-primary">Vai<span class="ion-ios-arrow-right pl-2"></span></button>
        	</div>
        </div>
{{/items}}