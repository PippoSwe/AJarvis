{{#items}}
        <div id="list-keyword-{{keyword_id}}" class="list-group-item pb-5">
            <h4 class="list-group-item-heading float-left">{{keyword}}</h4>
            <div class="btn-group pull-right">
                <button type="button" data-id="{{keyword_id}}" class="delete btn btn-danger"><span class="ion-ios-trash pr-2"></span>Cancella</button>
            </div>
        </div>
{{/items}}